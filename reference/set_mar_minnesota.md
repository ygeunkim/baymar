# Minnesota Prior Specification

**\[experimental\]** Set Minnesota prior.

## Usage

``` r
set_mar_minnesota(kappa = set_kappa())

is.matmnspec(x)

is.bmarspec(x)

is.kappaspec(x)
```

## Arguments

- kappa:

  Shrinkage hyperparameter of MN scale

- x:

  Any object
